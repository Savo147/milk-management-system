/**
 * Cache tags, in one place so the side that caches and the side that clears
 * cannot drift apart over a typo.
 */

/** The dairy's name, logo and settings. Cleared when Settings is saved. */
export const BRANDING_TAG = "branding";
